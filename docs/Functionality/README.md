# Functionality (WHAT)

The FSD for the iOS app lives beside the code it governs:

**[`ios/BalloonHunter/BalloonHunterAppFSD.md`](../../ios/BalloonHunter/BalloonHunterAppFSD.md)**

It is bound here rather than moved: source comments cite its sections by name, and
the interface reference it depends on sits next to it —
[`ios/BalloonHunter/SondeHub_API_Reference.md`](../../ios/BalloonHunter/SondeHub_API_Reference.md).

`android/` is a separate product with its own specification and is outside this
plane set.
