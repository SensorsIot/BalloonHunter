# Raw run logs are megabytes each and are regenerated by re-running the tests.
# The curated excerpts and the campaign verdicts beside them are the evidence of
# record, and are tracked.
evidence/ui-cycle-*.csv
