// MapView.swift
// Main screen stub
import SwiftUI

struct MapView: View {
    var body: some View {
        Text("MapView Stub")
            .frame(maxWidth: .infinity, maxHeight: .infinity)
            .background(Color(.systemBackground))
    }
}
